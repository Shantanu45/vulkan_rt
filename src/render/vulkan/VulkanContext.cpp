#include "VulkanContext.hpp"

namespace vulkan_rt::render::vulkan
{
VulkanContext::VulkanContext(const VulkanContextConfig &config, SurfaceProvider &surface_provider)
{
  //load_vulkan_loader();
  //validate_requested_layers(config);
  //create_instance(config, surface_provider.required_instance_extensions());
  //load_instance_functions();
  //create_debug_messenger_if_enabled(config);
  //create_surface(surface_provider);
}
}
